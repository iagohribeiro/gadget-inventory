export class Gadget {
    constructor(public model:string, public brand:string, public serial:string, public coverage:string){
        this.model = model;
        this.brand = brand;
        this.serial = serial;
        this.coverage = coverage;
    }
}
